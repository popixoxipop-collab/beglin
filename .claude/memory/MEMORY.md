# vdsp-engine Memory

08-31: Finance/AEQ 등과 같은 급의 repo-isolation으로 전환 — 이전엔 전역
memory(`~/.claude/projects/-Users-xox/memory/`)에 있던 vdsp_*.md 11개를
전부 여기로 이전. 신규 vdsp-engine 관련 메모리는 앞으로 여기 저장할 것
(전역 저장 시 `repo-isolation-guard.py`가 차단+리다이렉트).

## 연구/조사 결과
- [turboquant_lut_router_sensitivity](turboquant_lut_router_sensitivity.md) — TurboQuant/PolarQuant+Lloyd-Max 코드북: 가중치 재구성은 -8~12% 개선(저장공간 0), 그러나 MoE top-k 라우팅 민감도로 실제 forward-pass는 5/8 argmax(버그 아님, MoE-2b/4c와 동일 메커니즘)

## 장기 목표 / 진행중 트랙
- [vdsp_general_serving_engine_goal](vdsp_general_serving_engine_goal.md) — ★★★ 장기목표: vdsp→Apple Silicon GPU+MoE 서빙엔진(MLX/llama.cpp급). MoE-format safetensors 7단계 CLOSED. V5a/V5b/V5c 정확도게이트 PASS. V5c-fused: Bug1/2/3 전부 근본원인 발견+완전해결 — **KILL-GATE 최종 통과**(2026-08-31, 21.78→~52.91 tok/s, bar 48.34의 109%). V5d(배치 B-token GPU decode): B∈{8..64} 전 구간 **flipped=0**. F-4: global sort/unsort → **B=64 224.3 tok/s, llama.cpp의 1.24배**. V5e(ragged multi-step, prefill+decode 통합설계): 4번째 probe에서 Bug1과 같은 부류의 새 함정(rope offset이 H=1엔 맞고 H=16엔 조용히 틀림) 발견+수정, off-by-one 발견+수정 거쳐 **정확도 56/56 완료**, 메모리 10.05GB(ceiling 12.71GB) 확인. 패턴은 hw-kernel-vendoring 스킬에 일반화 기록. V5f(같은 워크로드 CPU vs GPU 3회 통제비교): **GPU ~4.3배 우세**(2.77→11.87 tok/s), 격차가 V5d B=64(224.3)보다 작은 이유(진짜 batched-causal prefill 미구현)까지 설명 완료. V5g(진짜 batched-causal prefill): mlx_moe.cpp 변경 없이 기존 per-row mask 재활용으로 구현, 첫 실측은 처리량 무변화처럼 보였으나 prefill 단독계측으로 MLX shape-JIT 콜드스타트(7407ms) 원인 규명 후 warmup 추가 → **421.80ms(17.6배↓), 최종 평균 86.5 tok/s(V5f GPU대비 7.3배, CPU대비 ~31배)**. V5h(GPU 온라인 admission 스케줄러): CPU의 검증된 MoE-4b 스케줄러(요청테이블+FIFO arrival+슬롯재사용+budget-chunked prefill)를 GPU에 최초 이식, mlx_moe.cpp 변경 없음. CPU를 ground truth 삼아 두 워크로드(기본+staggered arrival) **32/32 요청·228/228 토큰 완전일치**, invariant 위반 0건. 처리량 평균 99.58 tok/s(편차 최소, 2-pass 전체시뮬 웜업 덕분) — CPU 정확도기준모드 대비 ~62배, CPU 실서빙모드 대비 ~35.7배. V5a~h 트랙 전체 COMPLETE. V5i(GQA 모델 일반화, MLA전용→OLMoE): Qwen3-30B-A3B는 하드웨어원천불가(18.5GB>12.71GB ceiling) 확인 후 OLMoE로 전환, Phase A(export+CPU검증, rel_l2~2e-07)+Phase B(신규 `mlx_gpu_gqa_config/layer0`, NeoX rope 첫 사용, B=1 3-way 교차검증 rel_l2~2e-07) 통합완료 — B=1/layer-0 스코프 COMPLETE. V5j(멀티레이어 전체모델, V5c급): 풀 16레이어 export(exporter가 post_attention_layernorm 빠뜨린 진짜버그 발견+수정)+CPU검증(13/13 argmax)+신규 `mlx_gpu_gqa_layer_step_lazy/forward_finalize`(D1 lazy-graph broadcast probe max_abs_diff=0.0)+`run_moe_gpu_gqa_fused_gate()` — GPU vs 실MLX 13/13 argmax rel_l2 5e-3~1.1e-2 COMPLETE, 처리량 106.93 tok/s(관측치만). ★★CPU cross-check divergence 근본원인 확정+수정 완료(후속세션): 11개 가설(K/V배열오염·비결정성·스레드race·routing_out·임베딩x·h/가중치·Metal-init FPU·raw AF-blob바이트·MLX바인딩·ARM64 FPCR·MallocPreScribble uninit-read) 전부 반증 후, GPU eval을 0개로 줄이는 bisection(`MAXGPULAYERS0=0`)에서도 여전히 divergence 재현 → GPU 무관임을 확인하고 두 드라이버의 C코드 자체를 직접 diff. **`run_moe_gpu_gqa_fused_gate()`(D5)가 `moe_init_rope_gqa()` 호출 누락**(다른 모든 GQA 진입점은 호출) — NeoX rope 주파수테이블 `g_moe_rope_inv`가 malloc만되고 채워지지않은 미초기화 메모리, pos=0은 회전각=pos*freq=0이라 무관, pos≥1부터 오염 — 패턴과 완벽히 일치. 1줄 수정(`moe_init_rope_gqa();` 추가) 후 D5 내부 K_row_sum이 C4와 9자리까지 완전동일(356.745603780), pos8/10 argmax flip도 해소. V5j-batch(V5d대응, 배치 B-token GPU decode): D3/D4가 이미 `g_fused_B`로 전부 batch-generic하게 짜여있어(직접 코드확인, 가정아님) mlx_moe.cpp/.h 변경 0건 — `run_moe_gpu_gqa_batch_gate()` 신규진입점(D5설정블록+`moe_init_rope_gqa()`수정포함)+macstudio에서생성한 실 OLMoE 64토큰 corpus만 추가. CPU naive-vs-gather 크로스체크를 게이트 내부에 필수FATAL로내장(MLA는V5d전에이미검증됐지만GQA는이번이최초검증이라). B∈{1,8,16,24,32,48,64} 전구간 **flipped=0**+naive-vs-gather 100%일치+처리량 105.8→472.6 tok/s(B=64서~4.5배)+determinism확인(B=64 2회재현 byte-identical). V5j-ragged(V5e/V5g/V5h대응, ragged/online GQA batching): Phase A(macstudio 실캡처+EOS=50279확인+scatter/take/group-broadcast 격리probe max_abs_diff=0.0)+Phase B(신규 `mlx_gpu_gqa_cbatch_layer_step_lazy/finalize` — 초회실측 0/56 완전실패, `mx::fast::rope()`의 `freqs=`(array offset)와 `base=`(scalar) 비등가성이 array-offset과 결합시 근본원인(1.12 diff vs 0.0)으로 격리probe 확정+수정→56/56)+Phase C(batched-causal prefill, mlx_moe.cpp 추가변경 0건 재확인, 56/56, 211.4 tok/s, Phase B decode-only 111.9대비 ~1.9배)+Phase D(GPU online admission scheduler, mlx_moe.cpp 추가변경 0건 — 기존 CPU `run_moe_cbatch_verify_mode()`의 online분기는 DeepSeek 토큰ID 하드코딩이라 재사용불가해 신규 CPU ground-truth 함수 `run_moe_gqa_cbatch_online_cpu_gate()` 작성, 기본+staggered arrival 2워크로드 CPU/GPU 요청·토큰·스케줄링불변량(admit_step/queue_wait/steps) 전부 byte-identical+determinism재확인) — V5j-ragged COMPLETE. ★사용자요청 사후 일반화 리팩터: 4개 GQA cbatch게이트(B/C/D-CPU/D-GPU)가 독립복붙하던 config-loading블록(~65줄×4)+워크로드리터럴테이블+EOS매직넘버(50279)를 공유헬퍼(`MoeGqaCbatchCtx`/`moe_load_gqa_cbatch_config()`)+공유테이블(`g_moe_gqa_cbatch_*`)+config필드(`arch_config_moe.txt EOS_TOKEN_ID`, exporter도 `tokenizer.eos_token_id`로 갱신)로 추출 — 이미 shipped된 D5/MLA V5e~h는 Rule3에 따라 미변경, 리팩터후 전 게이트 재검증 byte-identical. V5a~j GQA/OLMoE 트랙 전체 COMPLETE(MLA V5a~h와 동등 스코프). ★llama.cpp OLMoE 베이스라인(매 V5j 라운드마다 deferred였던 항목) 최종 CLOSED: 이 llama.cpp 체크아웃(`d83f72d`)의 Python HF→GGUF 컨버터엔 OLMoE 클래스가 없어(C++ core는 `LLM_ARCH_OLMOE` 지원하는데도) 로컬변환 대신 공식 `allenai/OLMoE-1B-7B-0125-GGUF` Q4_0(3.93GB, vdsp 자체 q4g64와 같은 균일4비트 계열이라 DeepSeek때 쓴 Q4_K_M보다 오히려 더 apples-to-apples) 다운로드, DeepSeek/MLA F-4와 동일 방법론(`llama-batched -n 48 -np N -kvu --perf`)으로 V5j-batch와 같은 B스윕 재현. 결과: B=1/8서 거의 동률(0.97/0.99), B=16/24서 llama.cpp가 근소우위(~5%), B=32/48/64서 vdsp우위(1.25x/1.13x/1.06x) — MLA/DeepSeek F-4의 "처음엔 대패→sort수정후 1.24x 완승" 패턴과 다르게 vdsp가 5%이상 뒤진적이 없고 역전폭이 B증가에 따라 좁아짐(1.25→1.06). llama.cpp 자체의 B=32 비단조 dip(331.57→~268→351.46, 3회재현 확인)이 vdsp B=32우위폭을 부풀린 원인이라고 정직히 명시. GGUF파일이 vdsp AF blob보다 ~9%작다는 미통제변수도 명시(group32 vs 64). 상세: RESULTS.md "V5j"/"V5j-batch"/"V5j-ragged"/"llama.cpp OLMoE baseline" 섹션. ★V5k(실 생성 entry point + 기본값 승격, 사용자의 "GPU MoE 경로를 기본 서빙경로로 승격" 요청 응답): 조사 결과 dense `greedy`에 대응하는 MoE 진입점이 아예 없었음(15개 GPU게이트+3개 CPU verify-mode 전부 teacher-forced dump이거나 고정 참조 대조용, online스케줄러 게이트 2개도 argc/argv를 버리고 고정 8프롬프트만 재생) — "승격할 대상이 없다"는 걸 먼저 확인 후 신규 구축. Phase 1(`run_moe_gpu_gqa_generate_gate`, `QWEN_MOE_GPU_GQA_GENERATE=1`): D5를 구조적으로 미러링(CPU crosscheck+ref_logits 요구 제거, EOS-stop 추가), dense의 `load_ids()`/`QWEN_MOE_PROMPT`(raw int32) 재사용, 공유 config헬퍼 재사용, D3/D4 primitive 무변경 재사용. ★버그: D5의 `mlx_gpu_bind_af()` 바인딩 루프를 실수로 같이 제거해 초회 전체실패(텐서조회 예외→catch(...)→조용히 0) — 복원 후 해결. 검증: 실 8프롬프트 중 7/8이 12/12 완전일치, 8번째(slot2)는 index10부터 갈림(이 프로젝트에서 slot2를 index8이상 검증한 적이 없었던 최초 구간 — V5j-ragged Phase B도 moe_cbatch_gen[2]=8이라 index7까지만 검증) — determinism 재현 확인, D5 자체 rel_l2~1e-2 오차대역과 일치하는 근접타이 argmax로 판단, 버그아님 — **83/84 토큰 정확**. 포지션캡(32) graceful early-stop도 직접 확인(25/30). Phase 2(`run_moe_gpu_gqa_generate_default_mode`): weights_moe존재+ATTN_KIND==GQA(peek, MLA는 안전하게 fallthrough)+GPU가용+프롬프트해석가능 전부 만족시에만 Phase1 게이트로 setenv위임, 하나라도 실패시 완전 무변경 fallthrough — QWEN_SME2 승격(공유함수내 unset-분기 변경)보다 blast radius 큼을 명시하고 graceful tier로 설계, 무env var시 기존 dump와 byte-identical 확인+QWEN_MOE_PROMPT만 설정시 자동발동 확인. ★MLA대응(V5k Phase 1b/2b, 사용자가 바로 이어서 요청)도 완료: `run_moe_gpu_generate_gate()`(`QWEN_MOE_GPU_GENERATE=1`)를 V5c-fused(`run_moe_gpu_fused_gate`) 구조 그대로 미러링, MLA 전용 `mlx_gpu_layer_step_lazy`/`mlx_gpu_forward_finalize`/`mlx_gpu_mla_config` 무변경 재사용(GQA버그 교훈으로 바인딩루프 처음부터 포함). DeepSeek 실 EOS=100001을 이 세션의 기존 관행("100001")을 그냥 믿지 않고 macstudio 실모델 config.json/generation_config.json 직접 대조로 재확인(Data-First Numerics) 후 arch_config에 `EOS_TOKEN_ID=100001` 추가. MLA전용 config헬퍼는 안 만듦(호출자가 이거 하나뿐이라 공유의미없음, Rule3 그대로 inline 1회). 검증: 실 8프롬프트(`moe4a_ref_generation.json`) **8/8 완전일치 96/96 토큰(GQA의 83/84보다도 깨끗함 — MLA가 이 프로젝트에서 가장 많이 검증된 모델이라 설명됨)**, determinism/포지션캡/기본값승격(Phase2b, ATTN_KIND peek으로 GQA쪽과 order-independent 공존)/no-envvar fallback 전부 직접확인, V5c-fused(8/8, 52.5tok/s kill-gate 통과)+V5e cbatch(56/56) 회귀무영향. **V5k는 이제 GQA+MLA 두 토폴로지 모두 COMPLETE**. 상세: RESULTS.md "V5k"/"V5k Phase 1b/2b" 섹션. ★V5l(V5j-ragged Phase D 온라인 admission 스케줄러에 실 프롬프트 연결, GQA만 이번라운드): `QWEN_MOE_CB_PROMPT_MANIFEST` 신설(corpus/manifest를 로드시점 단일테이블로 통합, 미설정시 byte-identical 보장). 설계검증 중 `moe_cb4b_admit_guard()`가 `rq_out`의 실제 선언폭(`MOE_CBATCH_KNEW=12`)을 전혀 모른다는 잠재 버퍼오버플로우 발견 → `rq_out` 폭을 guard의 실제 상한(`MOE_CBATCH_MAXPOS=32`)로 확장해 근본해결, **ASan으로 수정전 실제 global-buffer-overflow 재현+수정후 클린 통과 양쪽 다 실증**(이론적 우려가 아니라 실재 버그였음을 최고강도로 증명). 검증 5개(기본회귀/8개corpus재현/MC=10+R=20 wraparound/ASan) 전부 통과. ★MLA 대응도 즉시 이어서 완료: 동일 매니페스트 메커니즘 이식+corpus는 이미 함수로컬이라 hoisting 불필요+EOS 100001 하드코딩→`MOE_EOS_TOKEN_ID`(config-driven)로 정리+로더함수 `moe_cbatch_load_manifest`로 개명해 GQA/MLA 공유(중복없음), ASan으로 GQA와 동일 형태 오버플로우 재현+수정확인. **GQA+MLA 두 토폴로지 모두 COMPLETE**. ★GQA CPU twin에도 매니페스트 이식 완료(사용자 요청): 로더함수가 CPU twin보다 뒤에 정의돼있어 실제 컴파일에러 발생→함수를 물리적으로 앞으로 이동해 해결. 검증(4)가 질적으로 격상됨 — 이전엔 V5k 단일시퀀스게이트와만 대조했지만 이번엔 **GQA GPU 온라인게이트의 실제 실행결과와 직접 대조**(req/slot/tokens/steps 전부 완전일치) — 온라인 스케줄러 자체의 CPU=GPU를 임의 매니페스트 워크로드에서 최초로 증명. ASan도 CPU twin 자체에서 재현+수정확인. ★MLA CPU twin도 완료(`run_moe_cbatch_verify_mode()`의 online분기) — 이 함수가 `#ifdef QWEN_GPU_MLX` 가드 밖이라 dense-only 빌드도 직접 재확인함(이전 3라운드와 다른 리스크 프로필). 로더함수 재이동(파일 최상단), EOS는 이 함수의 "config는 caller가 로드" 설계원칙 지켜 이번엔 미변경(값 자체는 안전). 검증(4)가 MLA GPU게이트 실결과와 완전일치, ASan(5)도 pre/post-fix 확인. **`QWEN_MOE_CB_PROMPT_MANIFEST`가 4개 온라인게이트(GQA/MLA × CPU/GPU) 전부 커버 완결**. ★`load_ids()` 입력검증도 완료(사용자 요청): 4개 게이트가 공유하는 로더 한 곳에 `0<=id<MOE_VOCAB` 검사 추가해 4곳 동시적용, 4개 게이트 전부 정상 매니페스트 재실행 완전동일(회귀無)+범위밖/음수 토큰 양성테스트로 FATAL 정확히 트리거 확인. **V5l 매니페스트 기능 완전 마무리**. 상세: [vdsp_general_serving_engine_goal](vdsp_general_serving_engine_goal.md), RESULTS.md "V5l"/"V5l MLA mirror"/"V5l GQA CPU twin"/"V5l MLA CPU twin"/"V5l: manifest loader input validation" 섹션

- [vdsp_general_serving_engine_goal](vdsp_general_serving_engine_goal.md) 추가 — D-roadmap-2/3(2026-09-01): 39/40 attention role조합 bits=16 필요, local-vs-upstream 6.7-11.3배로 상류누적 확정→블랭킷 전레이어 승격 채택. wikitext 84%→5.8% 정정. ★DeepSeek-V2-Lite 포팅: OLMoE의 "초반레이어 위험" 패턴이 정반대로 뒤집힘(DeepSeek는 깊은레이어 24,25,20-23이 위험) — 아키텍처특이적, 일반화 불가 확정. ★D-roadmap-3 flag-and-log-only 근접동점 텔레메트리 프로토타입 shipped+실행검증완료+threshold 실측기본값(0.5, bits4-flip 최대margin=0.4786 바로위) 채택으로 FATAL가드 제거. ★★correction 경로(bits=16 전체포지션 재계산, OLMoE/GQA)도 사용자 선택으로 이어서 구현+검증완료 — 정확도 실사례(380오답→correction 831=genuine bf16 정확일치), production AF-blob 자체유발 인공근접동점 발견. git커밋은 미실행. ★MLA확장(같은세션 후속, 사용자의 "적응형엔진 왜 테스트 안해" 지적으로 착수): correction경로 3곳 코드수정+실라이브테스트 중 진짜 SIGSEGV 발견(GQA전용 진단코드가 g_moe_lt_hi.k_proj를 MLA에서 NULL역참조)+수정, D-deepseek-precint-4의 int4 flip(pos6 8872↔344)은 실사용 AF-blob 경로에선 재현 안 됨을 정직히 보고(margin=0.7116, 애초 안 걸릴 위치)—메커니즘 구조적 정상은 강제트리거로 확인. 회귀/RSS(+1.25GiB) 실측완료. RESULTS.md "D-deepseek-precint-5" 신규. ★★D-roadmap-4(2026-09-02, 같은세션 후속): 사용자가 "SLAM MAP처럼 role×layer 단위 실시간 귀속+Supabase 영속화+폐루프 승격" 요청 → 경쟁토큰 캡처+로컬JSONL로깅+108조합 role×layer attribution(포인터스왑 재사용, 신규텐서로딩無) 구현+실측. **5개 real flip 중 4개(80%)가 단일 role/layer로 설명 안 됨**(local-vs-upstream 우세 재확인, 라이브 증거로). 비용실측(pos비례, pos=15는 8분+ 미완주)으로 MAX_EVENTS/MAX_POS 상한 신설. ★★★사고: bob(16GB)에 무거운 프로세스 3개 동시실행(내작업+fork2개)→실제 재부팅(uptime "up 7일"→"up 6:43"으로 확정)+수시간 복구불가. 재발방지로 전역 hook `bob-macstudio-concurrent-load-guard.py` 신설+라이브검증(실제 Bash 호출 차단까지 확인). Supabase는 project_ref=btdjbfgqzglucifcnuoc("beglin") MCP 등록했으나 세션 재시작 전엔 승인 안 됨—스키마/push스크립트는 준비완료, 미실행. ROI우선순위(사용자 지시) C(자동role_bits생성)/D(FFN 16·32bit허용) fork 완료(둘다 진짜 버그 발견: C=주석파싱무음드롭, D=allow_f32뿐아니라f32_as_af도같이바꿔야함), Phase6(g_moe_lt_active 폐루프) fork 완료(로컬컴파일검증, bob라이브검증대기). ★★사용자가 "센서 왜 미루냐, 지금 필요하다" 직접 지적 → attribution을 attention전용(108)에서 dense/shared FFN까지 확장(189조합)+라우팅센서(`g_moe_routing_capture`, 실제 physical slot 배열로 스레딩 — fork가 스스로 compact-index vs slot 버그 발견+수정) 같은 라운드에 즉시 구현. **실측 검증 완료**: req5/pos4 재현시 13/108→17/189로 hit 증가, 신규 4개 전부 shared_gate/up/down_proj — FFN이 실제로 일부 flip에 기여한다는 라이브 증거. 라우팅센서도 실제값(0아님) 캡처 확인. 표준회귀 byte-identical 확인. WikiText-2 4청크(aa~ad) 전부 정상완주(순차실행, hook으로 동시실행 차단 검증됨). ★확장판(dense/shared+라우팅센서)으로 4청크 재검증도 완주: 78이벤트/6flip/89hit, **dense도 실제 기여 확인**(gate/down 각1건, 이전 단일표본에선 0이었음), shared계열 총32건 vs attention총55건 — 사용자의 "FFN도 독립진단 필요" 요구가 실측으로 정당화됨. hit분포 극단적(0~2가 4건, 87/189가 1건). ★★로컬(M1 Max)에 bob과 TB4/USB4 직결(실측 243MB/s, 기존 로컬경유 대비 ~500배) 성공, AF-blob+bf16세이프텐서(41GB) 로컬로 가져와 `/Users/xox/vdsp_local_data/`에 원샷실험 환경 구축(bob 부하 无) — req5/pos4의 17개 hit 합산 모노톤성 1차검증(union_argmax=473=정답, 성립 확인). ddmin 최소충분집합탐색 계획서 별도 작성(`.claude/history/2026-09-02_minimal-sufficient-subset-search-plan.md`). ★★D-roadmap-4 최종 마무리: 두번째 모노톤성 데이터포인트(req32/pos8, 87개hit 합산→union_argmax=245=정답, 2/2 성립)도 로컬에서 확인. **Phase6(g_moe_lt_active 폐루프승격)도 bob에서 라이브검증 완료**—재시작 없이 실제 승격 로그(`PROMOTED to bits=16 -- permanent, no restart`) 확인, 표준회귀 byte-identical. ROI-C 완전종료(미스터리=코드버그 아니라 재부팅으로 `/tmp`파일유실+stderr버퍼링, script로 pseudo-TTY 잡아 재확인 완료). ROI-D 완전종료(표준회귀+실효과A/B 둘다 통과, ROADMAP "FFN무반응" 재확인, 성과 부풀리지 않고 정직보고). 남은 건 Phase1/4(Supabase, 세션재시작 필요)와 ROI-G(사용자확인 필요)뿐.
- ★★★D-roadmap-4 후속 마무리(2026-09-02, 같은세션): Phase1/4 실행 완료(REST는 DDL 불가 확인 후 psql Session Pooler로 스키마+191행 seed+실data push, 독립 SELECT로 검증). `moe_sme2_ensure_ready()` bits미체크 hazard 발견+수정+**실제 재현**(bob에서 진짜 승격된 bits=16 텐서가 취약 코드 진입 확인: ng=0→malloc(0), scale/bias_off=-1 → OOB 역참조 직전까지 감, 벤더 repack 커널의 우연한 rc=-1 거부만이 출력 보호 — 운으로 살아남은 것, fix로 그 의존 제거). 다른 세션(D-d5-*, GQA/DWQ) 발견: `D-d5-11`(moe_cbatch_step이 g_moe_lt_cur 안 읽어 Phase6 승격이 서빙에 한번도 반영 안 됐던 버그) 덕분에 내 hazard가 실전 도달 가능해짐 — 세션간 발견이 서로를 검증. ROI-G Phase1(Opus계획서 §Phase1): `tools/quant_sim_n.py`(stdlib-only group-64 RTN 시뮬레이터)+`qwen_infer.c` opt-in override(`QWEN_MOE_ATTRIB_SIM_*`, 회귀無)로 req5/pos4 3개 role×layer × n=2..16 45회 실측. **핵심결과: 3개 중 2개가 비단조**(낮은 n에서 통과→더 높은 n에서 재실패→더 높은 n에서 재통과) — Opus계획의 M-scalar 단조성 가정(§3.3 이분탐색 전제)이 실측으로 반증됨, Phase2 설계 재검토 필요. knee 3/4/4(전부 non-tier거나 tier), q_proj/L5 안정knee=6·kv_a/L13 안정knee=7 — tier(4/8/16) 밖. Supabase 스키마도 `moe_role_precision_state` PK를 (role,layer)→(model,role,layer)로 마이그레이션(다른세션 macstudio가 다른모델 push할 크로스모델 오염 위험 방지, 191행 무손실 백필 확인).

- ★vdsp GPU 서빙층 vs MLX 순정 B=1 벤치마크(2026-09-02, 별도세션): 사용자가 "vdsp의 자체 스케줄링/서빙을 얹은 MLX" vs "MLX 순정(mlx_lm.generate)"으로 명확히 재정의 후 실측. xox/macstudio 둘다 무관한 무거운 작업 중이라 bob(M4, 유일한 SME2기기)에서 진행, bob의 `vdsp_m4_bench`도 다른세션이 리팩터 중이라 건드리지 않고 fresh clone(`vdsp_m4_bench_sme2/vdsp_fresh`) 사용. **이 저장소에 GPU/MLX 빌드 스크립트가 원래 없었음**(README는 CPU전용) — pip mlx wheel의 C++ 헤더/dylib로 CMake 빌드 신규 구축, 함정 2개(CMake `ASM` 언어 미지정시 .S파일 조용히 누락 / `QWEN_GPU_MLX` define 안 하면 GPU게이트 전부 조용히 컴파일 제외) 발견+해결. 모델은 `mlx-community/OLMoE-1B-7B-0125-4bit`(vdsp 자체 기준 체크포인트 0125+4bit 양자화와 정확히 매칭, bob 로컬 풀프리시전 0924는 메모리부족으로 OOM 확인 후 배제). **결과: B=1에서 MLX 순정 128.33 tok/s > llama.cpp 108.83 > vdsp 105.8 — vdsp가 셋 중 제일 느림**(vdsp/MLX=0.82). mlx_lm.generate는 동시요청 서빙 개념 자체가 없어 vdsp의 B=32~64 동시처리 우위(472.6 tok/s)와 직접 비교 불가 — 결론: vdsp 서빙층의 진짜 가치는 "더 빠른 추론"이 아니라 "한 기기 동시 다중사용자 서빙"으로 청구를 좁혀야 함(단일스트림 속도 주장은 근거 없음, 데이터가 반대를 보여줌). config.json 심볼릭링크 경유 실수(원본에 `rms_norm_eps` 필드 추가됨, 애초 누락된 값이라 무해)도 투명하게 기록. 상세: RESULTS.md "vanilla MLX (mlx_lm.generate) baseline: B=1" 섹션. Status: OPEN — N-parallel MLX 동시처리 실측이 다음 단계.

- [vdsp_hiddenstate_signal_and_file_map](vdsp_hiddenstate_signal_and_file_map.md) — ★★ hidden-state-magnitude 신호 정확한 계산레시피(cleanlab 대신 채택, D1 WHY/COST/EXIT) + MoE 근접동점 조사 전체 파일지도(macstudio/bob/repo 스크립트·JSON·qwen_infer.c 함수명·RESULTS.md 섹션명). 새 아키텍처 이식 전 재검증 체크리스트 포함.

## 과거 완료/참고 트랙
- [vdsp_kleidiai_sme2_padding_mimicry](vdsp_kleidiai_sme2_padding_mimicry.md) — ARM KleidiAI SME2 프로덕션 통합 SHIPPED. Qwen B=64 +37%, Llama B=16 +26%, 지는 케이스 없음. M1Max가 M4보다 범용성능 우세(SME2 발동구간에서만 M4 역전)
- [vdsp_smallgicp_neon_omp_port](vdsp_smallgicp_neon_omp_port.md) — small_gicp Mac 포팅 + NEON 커널 5종 + libomp, KITTI N=100 회전오차 37%↓ 유의
- [vdsp_complex_pitfalls](vdsp_complex_pitfalls.md) — Apple vDSP 복소 커널 실측 함정 10개(M11~M41) + 판별기준, baseline ppl 11.5759
- [vdsp_m46_python_pipeline_and_d50_incident](vdsp_m46_python_pipeline_and_d50_incident.md) — M46(Python export/quantize/layout 일반화)+M47(Llama-3.1-8B RTN ppl 실측) 완료, RTN갭 67% 해소. 세션중 D50 실데이터 덮어쓰기 사고+복구 포함
- [vdsp_gptq_smoothquant_math_and_status](vdsp_gptq_smoothquant_math_and_status.md) — GPTQ+SmoothQuant 파이프라인 수학원리 + M48(Llama golden gate) 완료현황, 엔진/Python ppl 격차 미해결
- [vdsp_sme2_build_caller_plain_convention](vdsp_sme2_build_caller_plain_convention.md) — SME2 빌드 시 호출자는 항상 plain(-march 없이) 컴파일해야 하는 컨벤션, M4는 SVE를 SME2 스트리밍모드 안에서만 지원(lldb로 확정)
- [vdsp_sme2_lazy_repack_serve_sigill](vdsp_sme2_lazy_repack_serve_sigill.md) — `QWEN_SME2_LAZY_REPACK=1`+serve모드 조합 SIGILL, GGUF무관 기존코드에서도 재현(미해결, 우선순위 낮음)
- [vdsp_sme2_paper_codex_review_final](vdsp_sme2_paper_codex_review_final.md) — vdsp SME2 arXiv 논문 동료검증 이력, Codex 10라운드 후에도 독립 리뷰가 §5.1 수치모순(3~5배) 새로 발견, 실측 재검증으로 수정완료. "충분검증" 판단은 잠정적이라는 교훈
- [vdsp_m42_complex_quant_no_go](vdsp_m42_complex_quant_no_go.md) — 브랜치 m42-complex-w2의 복소 widely-linear W2 양자화 결론=NO-GO, 미머지 방치. ★main의 M42(구조적 일반화, 별개 작업)와 이름만 같음 주의
- [vdsp_m42_structural_gen_qwen_score_spec](vdsp_m42_structural_gen_qwen_score_spec.md) — main에 머지된 M42(qwen_score.c/qwen_spec.c 구조적 일반화, M41 후속), 완료·독립검증·반영. m42-complex-w2의 동명 M42(NO-GO)와 다른 작업
